# 🚀 Полная инструкция по развертыванию приложения на сервере

## 📋 Обзор

- **Домен**: `rjexpressinc.io`
- **Frontend**: Vercel (Next.js)
- **Backend**: Railway (FastAPI)
- **Telegram бот**: уже настроен

---

## 🎯 ШАГ 1: Развертывание Backend на Railway

### 1.1 Регистрация и вход

1. Зайдите на https://railway.app
2. Нажмите **"Start a New Project"** или **"Login"**
3. Войдите через **GitHub** (рекомендуется)
4. Подтвердите доступ к репозиториям

### 1.2 Создание проекта

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите и выберите репозиторий: `Samir-pixel/RjExpressApp`
4. Railway начнет сканирование проекта

### 1.3 Настройка Backend сервиса

**Важно:** Railway может автоматически определить проект, но нужно настроить правильно:

1. После импорта репозитория, Railway покажет найденные сервисы
2. Если автоматически создался сервис:
   - Нажмите на него
   - Перейдите в **Settings**
   - Найдите **Root Directory**
   - Установите: `backend`
   - Нажмите **Save**

3. Если сервис не создался автоматически:
   - Нажмите **"+ New"** → **"GitHub Repo"**
   - Выберите репозиторий `Samir-pixel/RjExpressApp`
   - В настройках установите **Root Directory**: `backend`

### 1.4 Переменные окружения в Railway

1. В настройках сервиса перейдите в **Variables**
2. Нажмите **"+ New Variable"**
3. Добавьте следующие переменные **по одной**:

```env
TELEGRAM_BOT_TOKEN=8122687393:AAEY6jIPIojjPGHSQb9HS89zS1tXOW1klXs
```

```env
TELEGRAM_CHAT_ID=1385241437
```

```env
ENVIRONMENT=production
```

```env
PORT=8000
```

```env
ALLOWED_ORIGINS=https://rjexpressinc.io,https://www.rjexpressinc.io
```

**Важно:**
- Добавляйте переменные по одной
- Значения вводите без кавычек
- После добавления всех переменных, Railway автоматически перезапустит сервис

### 1.5 Получение Railway URL

1. После деплоя (может занять 2-5 минут) перейдите в **Settings**
2. Найдите раздел **Networking**
3. Нажмите **"Generate Domain"** или найдите **Public Domain**
4. Скопируйте URL (например: `rj-express-backend-production.railway.app`)
5. **ЗАПИШИТЕ ЭТОТ URL** - он понадобится для Vercel!

### 1.6 Проверка Backend

1. Откройте скопированный URL в браузере: `https://your-backend.railway.app/health`
2. Должен вернуться: `{"status":"ok"}`
3. Проверьте API документацию: `https://your-backend.railway.app/docs`
4. Должна открыться интерактивная документация Swagger

### 1.7 Обновление CORS (после получения Vercel URL)

После того, как получите URL от Vercel, обновите `ALLOWED_ORIGINS`:

1. Railway → Settings → Variables
2. Найдите `ALLOWED_ORIGINS`
3. Обновите значение:
```env
ALLOWED_ORIGINS=https://rjexpressinc.io,https://www.rjexpressinc.io,https://rjexpressinc.vercel.app
```
(Добавьте временный Vercel URL)

4. После подключения постоянного домена, обновите обратно:
```env
ALLOWED_ORIGINS=https://rjexpressinc.io,https://www.rjexpressinc.io
```

---

## 🎯 ШАГ 2: Развертывание Frontend на Vercel

### 2.1 Регистрация и вход

1. Зайдите на https://vercel.com
2. Нажмите **"Sign Up"** или **"Log In"**
3. Войдите через **GitHub** (рекомендуется)
4. Подтвердите доступ к репозиториям

### 2.2 Создание проекта

1. Нажмите **"Add New..."** → **"Project"**
2. Найдите репозиторий `Samir-pixel/RjExpressApp`
3. Нажмите **"Import"**

### 2.3 ⚠️ КРИТИЧЕСКИ ВАЖНО: Настройка Root Directory

**НЕ НАЖИМАЙТЕ "Deploy" СРАЗУ!**

1. После импорта репозитория вы увидите страницу настройки проекта
2. Прокрутите вниз до раздела **"Configure Project"**
3. Найдите поле **"Root Directory"**
4. Нажмите на поле и введите: `frontend`
5. **Убедитесь, что значение точно `frontend`** (без слеша `/`, без точек `./`)
6. Нажмите **"Continue"** или **"Save"**

**Проверка:**
- Framework Preset должен автоматически определиться как **Next.js**
- Build Command должен быть: `npm run build`
- Output Directory должен быть: `.next`

### 2.4 Переменные окружения в Vercel

1. Найдите раздел **"Environment Variables"**
2. Нажмите **"Add"** для каждой переменной:

**Переменная 1:**
- Name: `BACKEND_URL`
- Value: `https://your-railway-backend-url.railway.app`
  (Замените на реальный URL из Railway!)
- Environment: `Production`, `Preview`, `Development` (выберите все три)

**Переменная 2:**
- Name: `NODE_ENV`
- Value: `production`
- Environment: `Production`, `Preview`, `Development`

**Переменная 3 (опционально):**
- Name: `NEXT_PUBLIC_SITE_URL`
- Value: `https://rjexpressinc.io`
- Environment: `Production`, `Preview`, `Development`

**Важно:**
- Замените `your-railway-backend-url.railway.app` на реальный URL из Railway (шаг 1.5)
- Значения вводите без кавычек
- Выберите все окружения (Production, Preview, Development)

### 2.5 Деплой

1. После настройки Root Directory и переменных окружения
2. Нажмите **"Deploy"**
3. Дождитесь завершения сборки (обычно 2-5 минут)
4. После успешного деплоя вы получите временный URL (например: `rjexpressinc.vercel.app`)

### 2.6 Проверка Frontend

1. Откройте временный Vercel URL в браузере
2. Должна загрузиться главная страница сайта
3. Проверьте форму "Join Our Team" - заполните и отправьте
4. Проверьте Telegram - должно прийти сообщение!

---

## 🎯 ШАГ 3: Подключение домена rjexpressinc.io

### 3.1 Подключение домена в Vercel

1. В Vercel перейдите в ваш проект → **Settings**
2. Найдите раздел **Domains**
3. Нажмите **"Add"** или **"Add Domain"**
4. Введите домен: `rjexpressinc.io`
5. Нажмите **"Add"**
6. Также добавьте: `www.rjexpressinc.io`

### 3.2 Настройка DNS записей

Vercel покажет инструкции по настройке DNS. Обычно нужно:

**Для основного домена (rjexpressinc.io):**

В панели управления доменом (где вы покупали домен) добавьте:

**Вариант A (если поддерживается):**
```
Тип: A
Имя: @ (или rjexpressinc.io)
Значение: 76.76.21.21
TTL: 3600 (или Auto)
```

**Вариант B (если A запись не работает, используйте CNAME):**
```
Тип: CNAME
Имя: @ (или rjexpressinc.io)
Значение: cname.vercel-dns.com
TTL: 3600
```

**Для www поддомена:**
```
Тип: CNAME
Имя: www
Значение: cname.vercel-dns.com
TTL: 3600
```

### 3.3 Ожидание распространения DNS

1. После добавления DNS записей подождите 5-60 минут
2. Vercel автоматически проверит DNS и выдаст SSL сертификат
3. В Vercel в разделе Domains вы увидите статус:
   - ⏳ **Pending** - ожидание проверки DNS
   - ✅ **Valid** - домен подключен и работает

### 3.4 Проверка работы домена

1. После того как статус станет "Valid", откройте `https://rjexpressinc.io`
2. Должен загрузиться ваш сайт
3. Проверьте, что форма работает
4. Проверьте Telegram - должны приходить сообщения

---

## 🔄 ШАГ 4: Обновление CORS в Railway

После подключения постоянного домена:

1. Зайдите в Railway → ваш проект → Settings → Variables
2. Найдите переменную `ALLOWED_ORIGINS`
3. Обновите значение:
```env
ALLOWED_ORIGINS=https://rjexpressinc.io,https://www.rjexpressinc.io
```
4. Railway автоматически перезапустит сервис

---

## ✅ Финальная проверка

### Проверьте все компоненты:

1. **Домен работает:**
   - ✅ `https://rjexpressinc.io` открывается
   - ✅ `https://www.rjexpressinc.io` открывается

2. **Frontend работает:**
   - ✅ Страница загружается
   - ✅ Все секции отображаются
   - ✅ Форма "Join Our Team" видна

3. **Backend работает:**
   - ✅ `https://your-backend.railway.app/health` возвращает `{"status":"ok"}`
   - ✅ `https://your-backend.railway.app/docs` открывается

4. **Интеграция работает:**
   - ✅ Форма отправляется без ошибок
   - ✅ Сообщения приходят в Telegram
   - ✅ В Telegram приходит форматированное сообщение с данными заявки

---

## 🔧 Обновление после изменений

### Обновление Backend:

```bash
# Изменения автоматически деплоятся через GitHub
git add .
git commit -m "Update backend"
git push origin main
# Railway автоматически пересоберет и задеплоит
```

### Обновление Frontend:

```bash
# Изменения автоматически деплоятся через GitHub
git add .
git commit -m "Update frontend"
git push origin main
# Vercel автоматически пересоберет и задеплоит
```

---

## 🐛 Решение проблем

### Проблема: 404 на Vercel

**Решение:**
1. Проверьте Root Directory = `frontend` в Settings → General
2. Пересоберите проект (Redeploy)
3. См. подробнее: [VERCEL_404_FIX.md](VERCEL_404_FIX.md)

### Проблема: Backend не доступен

**Решение:**
1. Проверьте логи в Railway: Deployments → View Logs
2. Проверьте переменные окружения
3. Убедитесь, что порт настроен правильно

### Проблема: Форма не отправляется

**Решение:**
1. Проверьте `BACKEND_URL` в Vercel (должен быть Railway URL)
2. Проверьте CORS в Railway (`ALLOWED_ORIGINS`)
3. Проверьте логи в Railway и Vercel
4. Откройте консоль браузера (F12) и проверьте ошибки

### Проблема: Telegram не работает

**Решение:**
1. Проверьте `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` в Railway
2. Убедитесь, что вы начали диалог с ботом
3. Проверьте логи в Railway на наличие ошибок

### Проблема: Домен не работает

**Решение:**
1. Проверьте DNS записи (может занять до 48 часов)
2. Проверьте статус в Vercel → Domains
3. Убедитесь, что домен добавлен в Vercel
4. Проверьте SSL сертификат (выдается автоматически)

---

## 📊 Мониторинг и логи

### Railway (Backend):

1. Зайдите в проект → Deployments
2. Выберите deployment → View Logs
3. Видите логи в реальном времени

### Vercel (Frontend):

1. Зайдите в проект → Deployments
2. Выберите deployment → View Function Logs
3. Видите логи API routes и ошибки

---

## 💰 Стоимость

- **Vercel**: Бесплатно (до 100GB bandwidth/месяц)
- **Railway**: 
  - Hobby план: $0.10 за $1 использования (примерно $5-10/месяц)
  - Pro план: $20/месяц (фиксированная цена)

---

## 📝 Чеклист развертывания

- [ ] Backend задеплоен на Railway
- [ ] Railway URL получен и записан
- [ ] Переменные окружения добавлены в Railway
- [ ] Backend проверен (health endpoint работает)
- [ ] Frontend задеплоен на Vercel
- [ ] Root Directory установлен в `frontend`
- [ ] Переменные окружения добавлены в Vercel (с Railway URL)
- [ ] Frontend проверен (страница загружается)
- [ ] Домен `rjexpressinc.io` подключен в Vercel
- [ ] DNS записи настроены
- [ ] Домен работает (SSL выдан)
- [ ] CORS обновлен в Railway
- [ ] Форма отправляется успешно
- [ ] Telegram сообщения приходят
- [ ] Все компоненты работают

---

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет доступен на `https://rjexpressinc.io` и полностью функционален!

Если возникнут проблемы - см. раздел "Решение проблем" выше или файлы:
- [VERCEL_404_FIX.md](VERCEL_404_FIX.md)
- [VERCEL_FIX.md](VERCEL_FIX.md)
- [DEPLOYMENT_RAILWAY_VERCEL.md](DEPLOYMENT_RAILWAY_VERCEL.md)

