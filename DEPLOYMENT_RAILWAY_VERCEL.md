# 🚀 Деплой на Railway (Backend) + Vercel (Frontend)

## 📋 Обзор

- **Frontend**: Vercel (Next.js) - `rjexpressinc.io`
- **Backend**: Railway (FastAPI) - API доступен по URL Railway

## 🎯 Шаг 1: Деплой Backend на Railway

### 1.1 Создание проекта на Railway

1. Зайдите на https://railway.app
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите репозиторий `Samir-pixel/RjExpressApp`

### 1.2 Настройка Backend сервиса

1. Railway автоматически определит проект
2. В настройках проекта:
   - **Root Directory**: `backend`
   - **Build Command**: (оставить пустым, используется Dockerfile)
   - **Start Command**: (оставить пустым, используется Dockerfile)

3. Или создайте новый сервис:
   - Нажмите **"+ New"** → **"GitHub Repo"**
   - Выберите репозиторий
   - В настройках укажите **Root Directory**: `backend`

### 1.3 Переменные окружения в Railway

В настройках сервиса (Settings → Variables) добавьте:

```env
TELEGRAM_BOT_TOKEN=8122687393:AAEY6jIPIojjPGHSQb9HS89zS1tXOW1klXs
TELEGRAM_CHAT_ID=1385241437
WHATSAPP_API_KEY=
ENVIRONMENT=production
ALLOWED_ORIGINS=https://rjexpressinc.io,https://www.rjexpressinc.io,https://rjexpressinc.vercel.app
PORT=8000
```

**Важно:**
- `ALLOWED_ORIGINS` должен включать ваш домен и временный Vercel URL
- После получения постоянного Railway URL, обновите `ALLOWED_ORIGINS`

### 1.4 Получение Railway URL

1. После деплоя Railway создаст публичный URL
2. Перейдите в **Settings** → **Networking** → **Public Domain**
3. Скопируйте URL (например: `rj-express-backend-production.railway.app`)
4. **Запишите этот URL** - он понадобится для Vercel

### 1.5 Проверка Backend

1. Откройте Railway URL в браузере: `https://your-backend.railway.app/health`
2. Должен вернуться: `{"status":"ok"}`
3. Проверьте API docs: `https://your-backend.railway.app/docs`

### 1.6 Настройка кастомного домена (опционально)

Если хотите использовать `api.rjexpressinc.io`:

1. В Railway: **Settings** → **Networking** → **Custom Domain**
2. Добавьте: `api.rjexpressinc.io`
3. Настройте DNS запись:
   - Тип: `CNAME`
   - Имя: `api`
   - Значение: Railway предоставит CNAME запись

## 🎯 Шаг 2: Деплой Frontend на Vercel

### 2.1 Создание проекта на Vercel

1. Зайдите на https://vercel.com
2. Войдите через GitHub
3. Нажмите **"Add New..."** → **"Project"**
4. Импортируйте репозиторий `Samir-pixel/RjExpressApp`

### 2.2 Настройка проекта

В настройках проекта:

**Build Settings:**
- **Framework Preset**: Next.js (автоматически определится)
- **Root Directory**: `frontend` (ВАЖНО! Установите это вручную)
- **Build Command**: `npm run build` (будет автоматически, если Root Directory правильный)
- **Output Directory**: `.next` (будет автоматически)
- **Install Command**: `npm install` (будет автоматически)

**Environment Variables:**
Добавьте переменные:

```env
BACKEND_URL=https://your-backend.railway.app
NODE_ENV=production
```

**Важно:** 
- Замените `your-backend.railway.app` на реальный URL из Railway!
- `NEXT_PUBLIC_BACKEND_URL` не требуется

### 2.3 Деплой

1. Нажмите **"Deploy"**
2. Vercel автоматически соберет и задеплоит проект
3. После деплоя вы получите временный URL (например: `rjexpressinc.vercel.app`)

### 2.4 Подключение кастомного домена

1. В настройках проекта: **Settings** → **Domains**
2. Добавьте домен: `rjexpressinc.io`
3. Также добавьте: `www.rjexpressinc.io`
4. Vercel покажет инструкции по настройке DNS

### 2.5 Настройка DNS

В панели управления доменом (где вы покупали домен) добавьте DNS записи:

**Для основного домена:**
```
Тип: A
Имя: @ (или rjexpressinc.io)
Значение: 76.76.21.21 (Vercel IP - может измениться, проверьте в Vercel)
```

**Для www поддомена:**
```
Тип: CNAME
Имя: www
Значение: cname.vercel-dns.com
```

**Альтернатива (если используется Cloudflare или другой DNS):**
- В Vercel будут показаны точные записи после добавления домена

### 2.6 SSL сертификат

Vercel автоматически выдаст SSL сертификат после настройки DNS. Это может занять несколько минут.

## 🔄 Шаг 3: Обновление CORS в Railway

После получения постоянного URL от Vercel:

1. Обновите переменную `ALLOWED_ORIGINS` в Railway:
```env
ALLOWED_ORIGINS=https://rjexpressinc.io,https://www.rjexpressinc.io
```

2. Перезапустите сервис в Railway (Settings → Restart)

## ✅ Шаг 4: Проверка работы

1. Откройте `https://rjexpressinc.io`
2. Проверьте форму "Join Our Team"
3. Отправьте тестовую заявку
4. Проверьте Telegram - должно прийти сообщение!

## 📝 Шаг 5: Обновление после изменений

### Обновление Backend:

```bash
# Изменения в backend автоматически деплоятся через GitHub
git add .
git commit -m "Update backend"
git push origin main
# Railway автоматически пересоберет и задеплоит
```

### Обновление Frontend:

```bash
# Изменения в frontend автоматически деплоятся через GitHub
git add .
git commit -m "Update frontend"
git push origin main
# Vercel автоматически пересоберет и задеплоит
```

## 🔧 Troubleshooting

### Backend не доступен

1. Проверьте логи в Railway: **Deployments** → выберите deployment → **View Logs**
2. Проверьте переменные окружения
3. Проверьте, что порт настроен правильно (Railway использует переменную `PORT`)

### Frontend не может подключиться к Backend

1. Проверьте `NEXT_PUBLIC_BACKEND_URL` в Vercel
2. Проверьте CORS настройки в Railway (`ALLOWED_ORIGINS`)
3. Проверьте что Railway URL правильный и доступен

### Домен не работает

1. Проверьте DNS записи (может занять до 48 часов)
2. Проверьте SSL сертификат в Vercel
3. Убедитесь что домен добавлен в Vercel

## 💰 Стоимость

- **Vercel**: Бесплатно (до 100GB bandwidth)
- **Railway**: $5/месяц (Pro план) или $0.10 за $1 использования (Hobby план)

## 📊 Мониторинг

- **Railway**: Логи в панели управления
- **Vercel**: Analytics в панели управления
- **Telegram**: Проверяйте получение сообщений

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет доступен на `https://rjexpressinc.io`

