# 🐳 Docker Setup Guide

Инструкция по запуску проекта через Docker.

## 📋 Требования

- Docker Engine 20.10+
- Docker Compose 2.0+
- Файл `.env` в папке `backend/` с настройками Telegram

## 🚀 Быстрый старт

### 1. Подготовка конфигурации

Убедитесь, что файл `backend/.env` содержит:

```env
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
WHATSAPP_API_KEY=your_whatsapp_key  # опционально
```

### 2. Сборка и запуск

```bash
# Сборка образов и запуск контейнеров
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d --build
```

### 3. Проверка работы

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000
- **API Health Check**: http://localhost:8000/health
- **API Docs**: http://localhost:8000/docs

## 📝 Полезные команды

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только frontend
docker-compose logs -f frontend
```

### Остановка контейнеров

```bash
# Остановить контейнеры
docker-compose stop

# Остановить и удалить контейнеры
docker-compose down

# Остановить и удалить контейнеры + volumes
docker-compose down -v
```

### Пересборка

```bash
# Пересборка без кеша
docker-compose build --no-cache

# Пересборка и перезапуск
docker-compose up --build --force-recreate
```

### Проверка статуса

```bash
# Статус контейнеров
docker-compose ps

# Проверка healthcheck
docker-compose ps
```

## 🔧 Конфигурация

### Переменные окружения

Переменные можно передавать через:
1. Файл `backend/.env` (читается автоматически)
2. Переменные окружения системы
3. Через `docker-compose.yml` (уже настроено)

### Порты

- **3000**: Frontend (Next.js)
- **8000**: Backend (FastAPI)

Для изменения портов отредактируйте `docker-compose.yml`:

```yaml
ports:
  - "3001:3000"  # Внешний:Внутренний
```

### Сеть

Контейнеры подключены к внутренней сети `rj-express-network`. Frontend обращается к backend по адресу `http://backend:8000`.

## 🏗️ Архитектура Docker образов

### Backend

- **Базовый образ**: `python:3.11-slim`
- **Многоэтапная сборка**: Нет (простой образ)
- **Безопасность**: Запуск от непривилегированного пользователя
- **Healthcheck**: Проверка `/health` endpoint

### Frontend

- **Базовый образ**: `node:20-alpine`
- **Многоэтапная сборка**: Да (deps → builder → runner)
- **Оптимизация**: Standalone output для минимального размера
- **Безопасность**: Запуск от непривилегированного пользователя
- **Healthcheck**: Проверка доступности frontend

## 🐛 Решение проблем

### Проблема: Контейнеры не запускаются

```bash
# Проверьте логи
docker-compose logs

# Проверьте статус
docker-compose ps
```

### Проблема: Frontend не может подключиться к Backend

1. Убедитесь, что оба контейнера в одной сети
2. Проверьте переменную `BACKEND_URL` в `docker-compose.yml`
3. Проверьте healthcheck backend - он должен быть healthy

### Проблема: Ошибки при сборке

```bash
# Очистите кеш и пересоберите
docker-compose build --no-cache
docker system prune -a  # Внимание: удалит все неиспользуемые образы
```

### Проблема: Порты заняты

Измените порты в `docker-compose.yml` или остановите процессы, использующие порты 3000 и 8000.

## 📦 Production Deployment

Для production рекомендуется:

1. Использовать `.env.production` файлы
2. Настроить reverse proxy (nginx/traefik)
3. Использовать secrets management
4. Настроить мониторинг и логирование
5. Использовать volume для persistent data

## 🔐 Безопасность

- Оба контейнера запускаются от непривилегированных пользователей
- Используются минимальные базовые образы (alpine, slim)
- Healthchecks настроены для автоматической проверки
- Секреты хранятся в `.env` файлах (не коммитятся в git)

## 📚 Дополнительные ресурсы

- [Docker Compose документация](https://docs.docker.com/compose/)
- [Next.js Docker deployment](https://nextjs.org/docs/deployment#docker-image)
- [FastAPI Docker deployment](https://fastapi.tiangolo.com/deployment/docker/)

