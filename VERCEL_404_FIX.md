# 🔧 Исправление ошибки 404 на Vercel

## Проблема
После деплоя на Vercel получаете ошибку `404: NOT_FOUND`.

## Причины и решения

### 1. Root Directory не установлен правильно

**Проверьте:**
1. Vercel → Settings → General → Root Directory
2. Должно быть: `frontend` (без слеша, без точек)
3. Если не установлено или неправильно - установите и пересоберите

### 2. Build Settings неправильные

**Проверьте:**
1. Vercel → Settings → General → Build & Development Settings
2. Framework Preset: должен быть `Next.js` (автоматически)
3. Build Command: `npm run build` (автоматически)
4. Output Directory: `.next` (автоматически)
5. Install Command: `npm install` (автоматически)

### 3. Переменные окружения

**Убедитесь, что установлены:**
- `BACKEND_URL=https://your-railway-url.railway.app`
- `NODE_ENV=production`
- `NEXT_PUBLIC_SITE_URL=https://rjexpressinc.io` (опционально)

### 4. Структура проекта

Убедитесь, что структура такая:
```
RjExpressApp/
├── frontend/
│   ├── package.json  ← должен содержать "next": "15.5.4"
│   ├── next.config.ts
│   ├── app/
│   │   ├── layout.tsx
│   │   └── page.tsx
│   └── ...
├── backend/
└── ...
```

## Пошаговое решение

### Шаг 1: Удалите проект (если уже создан)

1. Vercel → Settings → Delete Project
2. Подтвердите удаление

### Шаг 2: Создайте проект заново

1. Add New Project → Import Git Repository
2. Выберите `Samir-pixel/RjExpressApp`
3. **ВАЖНО:** НЕ нажимайте "Deploy" сразу!

### Шаг 3: Настройте Root Directory

1. Найдите раздел **"Configure Project"** или **"Root Directory"**
2. Установите: `frontend`
3. Нажмите **"Continue"** или **"Save"**

### Шаг 4: Добавьте переменные окружения

1. Найдите раздел **"Environment Variables"**
2. Добавьте:
   - `BACKEND_URL` = `https://your-railway-url.railway.app`
   - `NODE_ENV` = `production`
3. Нажмите **"Save"**

### Шаг 5: Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения сборки
3. Проверьте URL

## Альтернативное решение: Использовать Vercel CLI

Если веб-интерфейс не работает:

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# В корне проекта
cd frontend
vercel --prod
```

## Проверка после деплоя

1. Откройте URL проекта
2. Должна загрузиться главная страница
3. Проверьте форму отправки
4. Проверьте API: `/api/lead` должен работать

## Если ошибка остается

1. Проверьте логи деплоя в Vercel
2. Убедитесь, что `package.json` содержит Next.js
3. Проверьте, что все файлы закоммичены в Git
4. Попробуйте создать проект заново с нуля

