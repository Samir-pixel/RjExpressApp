# 🔧 Исправление ошибок Vercel

## Проблема 1: "No Next.js version detected"
Vercel не может найти Next.js, потому что Root Directory не установлен правильно.

## Проблема 2: "Could not read package.json: ENOENT"
Vercel ищет `package.json` в корне репозитория вместо папки `frontend`.

## Решение

### ⚠️ ВАЖНО: Root Directory должен быть установлен ДО первого деплоя!

### Вариант 1: Установить Root Directory в настройках проекта (Рекомендуется)

**При создании нового проекта:**
1. Импортируйте репозиторий
2. **НЕ нажимайте "Deploy" сразу!**
3. Нажмите **"Configure Project"** или найдите **"Root Directory"**
4. Установите значение: `frontend`
5. Нажмите **"Save"** или **"Continue"**
6. Теперь нажмите **"Deploy"**

**Если проект уже создан:**
1. Зайдите в Vercel → ваш проект → **Settings**
2. Перейдите в раздел **General**
3. Найдите **Root Directory**
4. Установите значение: `frontend`
5. Нажмите **Save**
6. Пересоберите проект (Redeploy)

### Вариант 2: Если Root Directory уже установлен

Проверьте:
1. **Settings** → **General** → **Root Directory** должно быть: `frontend`
2. Убедитесь, что в `frontend/package.json` есть `"next": "15.5.4"` в dependencies
3. Если Root Directory правильный, но ошибка остается:
   - Удалите проект в Vercel
   - Создайте заново
   - При создании укажите Root Directory: `frontend`

### Проверка структуры

Убедитесь, что структура проекта такая:
```
RjExpressApp/
├── frontend/
│   ├── package.json  ← должен содержать "next": "15.5.4"
│   ├── next.config.ts
│   └── app/
├── backend/
└── vercel.json
```

### После исправления

1. Убедитесь, что Root Directory = `frontend`
2. Добавьте переменные окружения:
   - `BACKEND_URL=https://your-railway-url.railway.app`
   - `NODE_ENV=production`
3. Нажмите **Redeploy**

## Альтернатива: Удалить vercel.json

Если проблемы продолжаются, можно удалить `vercel.json` полностью - Vercel автоматически определит Next.js при правильном Root Directory.

